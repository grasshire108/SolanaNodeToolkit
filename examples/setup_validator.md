# Setting Up a Solana Validator

1. Install the toolkit (see [README.md](../README.md)).
2. Configure your validator:
   ```bash
   cp config/validator.toml ~/solana-validator-config.toml
   # Edit ~/solana-validator-config.toml with your keys
